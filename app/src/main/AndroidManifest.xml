<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.uzh.math.lessonsApp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".lesson_1.L1_MainActivity" />
        <activity android:name=".lesson_2.L2_MainActivity" />
        <activity android:name=".lesson_2.SecondActivity" />
        <activity android:name=".lesson_2.SecondActivityWithResult" />
        <activity android:name=".lesson_4.L4_MainActivity" />
        <activity android:name=".lesson_3.L3_MainActivity" />
    </application>
</manifest>
